vec3 main()
{
    if (3 < 2)
    {
        return vec3(1, 1, 1);
    }
    else if (3 > 4)
    {
        return vec3(uv.y, uv.x, uv.x * 0.5) * vec3(3, 3, 3);
    }
    else
    {
        return vec3(1, 0, 0);
    };
};
